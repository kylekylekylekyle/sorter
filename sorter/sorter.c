#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include "sorter.h"
#include "mergesort.c"

int main (int argc; char** argv) {
	if (argv[1] != "-c")
	{
		printf("Error, needs flag -c\n");
		return 0;
	}
	return 0;
}
